import { Control, ControllerProps, FieldError } from 'react-hook-form';
import { CheckboxProps, FormControlLabelProps } from '@mui/material';
export declare type CheckboxElementProps = Omit<CheckboxProps, 'name'> & {
    validation?: ControllerProps['rules'];
    name: string;
    parseError?: (error: FieldError) => string;
    label?: FormControlLabelProps['label'];
    helperText?: string;
    control?: Control<any>;
};
export default function CheckboxElement({ name, validation, required, parseError, label, control, ...rest }: CheckboxElementProps): JSX.Element;
