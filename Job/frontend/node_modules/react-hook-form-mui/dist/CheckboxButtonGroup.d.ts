import { CheckboxProps } from '@mui/material';
import { Control, FieldError } from 'react-hook-form';
export declare type CheckboxButtonGroupProps = {
    options: any[];
    helperText?: string;
    name: string;
    required?: boolean;
    parseError?: (error: FieldError) => string;
    label?: string;
    labelKey?: string;
    valueKey?: string;
    onChange?: Function;
    returnObject?: boolean;
    disabled?: boolean;
    row?: boolean;
    control?: Control<any>;
    checkboxColor?: CheckboxProps['color'];
};
export default function CheckboxButtonGroup({ helperText, options, label, name, parseError, required, labelKey, valueKey, returnObject, disabled, row, control, checkboxColor, ...rest }: CheckboxButtonGroupProps): JSX.Element;
