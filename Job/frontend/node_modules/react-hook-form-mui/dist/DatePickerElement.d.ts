import { DatePickerProps } from '@mui/lab';
import { Control, ControllerProps, FieldError } from 'react-hook-form';
import { TextFieldProps } from '@mui/material';
export declare type DatePickerElementProps<TDate = unknown> = Omit<DatePickerProps, 'value' | 'onChange' | 'renderInput'> & {
    name: string;
    required?: boolean;
    isDate?: boolean;
    parseError?: (error: FieldError) => string;
    onChange?: (value?: TDate) => void;
    validation?: ControllerProps['rules'];
    parseDate?: (date: TDate) => string;
    control?: Control<any>;
    inputProps?: TextFieldProps;
    helperText?: TextFieldProps['helperText'];
};
export default function DatePickerElement({ isDate, parseError, name, required, parseDate, validation, inputProps, control, ...rest }: DatePickerElementProps): JSX.Element;
