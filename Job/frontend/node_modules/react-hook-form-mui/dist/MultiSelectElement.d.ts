import { Control, FieldError } from 'react-hook-form';
import { SelectProps } from '@mui/material';
export declare type MultiSelectElementProps = Omit<SelectProps, 'value'> & {
    menuItems: any[];
    label?: string;
    itemKey?: string;
    itemValue?: string;
    itemLabel?: string;
    required?: boolean;
    validation?: any;
    name: string;
    parseError?: (error: FieldError) => string;
    minWidth?: number;
    menuMaxHeight?: number;
    menuMaxWidth?: number;
    helperText?: string;
    showChips?: boolean;
    control?: Control<any>;
    showCheckbox?: boolean;
};
export default function MultiSelectElement({ menuItems, label, itemKey, itemValue, itemLabel, required, validation, parseError, name, menuMaxHeight, menuMaxWidth, minWidth, helperText, showChips, variant, control, showCheckbox, ...rest }: MultiSelectElementProps): JSX.Element;
