import { TextFieldProps } from '@mui/material';
import { Control, ControllerProps, FieldError } from 'react-hook-form';
export declare type SelectElementProps = Omit<TextFieldProps, 'name' | 'type' | 'onChange'> & {
    validation?: ControllerProps['rules'];
    name: string;
    options?: any[];
    valueKey?: string;
    labelKey?: string;
    type?: 'string' | 'number';
    parseError?: (error: FieldError) => string;
    objectOnChange?: boolean;
    onChange?: (value: any) => void;
    control?: Control<any>;
};
export default function SelectElement({ name, required, valueKey, labelKey, options, parseError, type, objectOnChange, validation, control, ...rest }: SelectElementProps): JSX.Element;
