import { TextFieldProps } from '@mui/material';
import { Control, ControllerProps, FieldError } from 'react-hook-form';
export declare type TextFieldElementProps = Omit<TextFieldProps, 'name'> & {
    validation?: ControllerProps['rules'];
    name: string;
    parseError?: (error: FieldError) => string;
    control?: Control<any>;
};
export default function TextFieldElement({ validation, parseError, type, required, name, control, ...rest }: TextFieldElementProps): JSX.Element;
