import { Control } from 'react-hook-form';
import { FormControlLabelProps } from '@mui/material';
declare type IProps = Omit<FormControlLabelProps, 'control'>;
export declare type SwitchElementProps = IProps & {
    name: string;
    control?: Control<any>;
};
export default function SwitchElement({ name, control, ...other }: SwitchElementProps): JSX.Element;
export {};
